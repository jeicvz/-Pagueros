<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>XPAY</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #ffffff; /* fondo blanco */
        padding: 40px;
        text-align: center;
      }

      h2 {
        font-size: 3rem;
        color: #2c3e50; /* azul marino oscuro para el título */
        margin-bottom: 30px;
      }

      form,
      .transacciones {
        background: #34495e; /* azul marino suave */
        padding: 30px;
        margin: 20px auto;
        border-radius: 12px;
        box-shadow: 0 0 12px rgba(0, 0, 0, 0.1);
        max-width: 500px;
        text-align: left;
        color: white; /* texto blanco dentro del cuadro */
      }

      label {
        font-size: 1.2rem;
        font-weight: bold;
        color: #ecf0f1; /* gris clarito para etiquetas */
        margin-bottom: 5px;
        display: block;
      }

      input,
      select {
        font-size: 1.1rem;
        padding: 10px;
        margin-bottom: 15px;
        width: 100%;
        border-radius: 6px;
        border: 1px solid #7f8c8d; /* borde gris tenue */
        background-color: #ecf0f1; /* gris muy claro para inputs */
        color: #2c3e50; /* texto azul oscuro en inputs */
      }

      .boton-centro {
        text-align: center;
      }

      button {
        background-color: #7f8c8d; /* gris tenue para botones */
        color: white;
        font-weight: bold;
        cursor: pointer;
        border: none;
        padding: 10px 20px;
        border-radius: 6px;
        font-size: 1.1rem;
        margin-top: 10px;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #95a5a6; /* gris más claro al pasar mouse */
      }

      .transacciones ul {
        padding-left: 20px;
      }
    </style>
  </head>
  <body>
    <h2>XPAY</h2>

    <form id="formularioPago" method="POST" action="/payment-process">
      <label for="email">Correo electrónico del cliente:</label>
      <input type="email" name="email" id="email" required />

      <label for="amount">Monto a pagar:</label>
      <input type="number" name="amount" id="amount" min="1" required />

      <label for="paymentMethod">Método de pago:</label>
      <select name="paymentMethod" id="paymentMethod" required>
        <option value="">-- Selecciona --</option>
        <option value="bankTransfer">Transferencia</option>
        <option value="paypal">PayPal</option>
        <option value="creditCard">Tarjeta de Crédito</option>
      </select>

      <div class="boton-centro">
        <button type="submit">Pagar</button>
      </div>
    </form>

    <form action="/payment-history" method="get">
      <div class="boton-centro">
        <button type="submit">Ver historial</button>
      </div>
    </form>
  </body>
</html>
